# .gitignore
*.ini
!inventory/hosts.example.ini
*.retry
collections/
*.log
.vault_pass
!.vault_pass.example

# Local inventory with real IPs — never committed to git
# prod.ini is already covered by *.ini above

# Vault — never committed, kept local only
group_vars/vault.yml
