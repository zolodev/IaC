[Unit]
Description=Daily apt download activities
Before=apt-daily-upgrade.timer

[Timer]
OnCalendar=*-*-* {{ daily_time['hour'] }}:{{ daily_time['minutes'] }}:00
RandomizedDelaySec=60m
Persistent=true

[Install]
WantedBy=timers.target